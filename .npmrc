# The default npm cache (~/.npm/_cacache) is read-only in the sandboxed dev
# environment, which makes `npm install` fail with EROFS. Use a project-local
# cache dir (writable) so plain `npm install` works.
cache=.npm-cache
